// Element selection through querySelector

// The querySelector() method returns the first element that matches a CSS selector.

let idSelector = document.querySelectorAll("#id-element");
console.log(idSelector);

let classSelector = document.querySelectorAll('.class-element');
console.log(classSelector);

let tagSelector = document.querySelectorAll("p");
console.log(tagSelector);

